@model IEnumerable<CategoryModel>

@{
    ViewData["Title"] = "Category Listed";
}

<h3>Danh sách danh mục của sản phẩm</h3>

<a asp-action="Create" asp-controller="Category" class="btn btn-primary btn-sm">Thêm danh mục</a>

<table class="table">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Tên danh mục</th>
            <th scope="col">Mô tả</th>
            <th scope="col">Tên URL của danh mục</th>
            <th scope="col">Trạng thái</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.Name</td>
                <td>@Html.Raw(item.Description)</td>
                <td>@item.Slug</td>

                <td>
                    @if(@item.Status == 1)
                    {
                        <span class="text text-success">Hiển thị</span>
                    }
                    else 
                    {
                        <span class="text text-danger">Ẩn</span>
                    }
                </td>

                <td>
                    <a asp-action="Edit" class="btn btn-warning btn-sm" asp-controller="Category" asp-route-Id="@item.Id">Sửa</a>
                    <a asp-action="Delete" class="btn btn-danger btn-sm" asp-controller="Category" asp-route-Id="@item.Id">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>